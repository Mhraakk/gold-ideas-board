# زرپک

بورد ایده‌های هدیهٔ طلای کارشده. هر کارت = قطعه + بستهٔ یکتا.

```bash
npm install
npm run typecheck
npm run dev
```

پیش‌نمایش روی همان میزبانی که پروژه بالا می‌آید. دستور `build` نسخهٔ انتشار است، نه ساخت فیچر.

دیل‌چک و منبع اونس واقعی‌اند. عدد طلای ساختگی نشان داده نمی‌شود.
